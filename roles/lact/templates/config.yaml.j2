# {{ ansible_managed }}

version: 6
daemon:
  log_level: info
  admin_group: wheel
  disable_clocks_cleanup: false
apply_settings_timer: 5
gpus:
{% for gpu in _gpus %}
  {{ gpu.id }}:
    {{ gpu.config | indent(4) }}
{% endfor %}
current_profile: null
auto_switch_profiles: false
